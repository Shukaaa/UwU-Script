// Variablen speichern Code-Example

uwu(str<Alter>,int<17>) // uwu zum definieren der Variablen
moan(owo(str<Alter>)) // moan zum printen, owo zur Ausgabe der Variable
sayonara(str<byebye>) // sayonara beendet das Programm mit einer Verabschiedung